[package]
name = "edp-fskit-bridge"
description = "Narrow C ABI bridge between macOS FSKit and the pure edp-core probe logic"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
edp-core.workspace = true
