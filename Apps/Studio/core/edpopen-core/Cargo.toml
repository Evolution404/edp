[package]
name = "edpopen-core"
version = "0.1.0"
edition = "2021"
description = "EDPOpen pure EDP format engine"

[dependencies]
serde = { version = "1", features = ["derive"] }
encoding_rs = "0.8"

[lib]
name = "edpopen_core"
crate-type = ["rlib", "staticlib"]
