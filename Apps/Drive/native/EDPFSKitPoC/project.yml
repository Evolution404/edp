name: EDPFSKitPoC
options:
  bundleIdPrefix: com.edp.drive
  deploymentTarget:
    macOS: "26.0"
settings:
  base:
    MACOSX_DEPLOYMENT_TARGET: "26.0"
    SWIFT_VERSION: "6.0"
    CODE_SIGNING_ALLOWED: NO
    CODE_SIGNING_REQUIRED: NO

targets:
  EDPFSKitHost:
    type: application
    platform: macOS
    deploymentTarget: "26.0"
    sources:
      - path: Host
    dependencies:
      - target: EDPFSKitExtension
        embed: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.edp.drive.fskit-poc
        PRODUCT_NAME: EDPFSKitPoC
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Host/Info.plist

  EDPFSKitExtension:
    type: extensionkit-extension
    platform: macOS
    deploymentTarget: "26.0"
    sources:
      - path: Extension
        excludes:
          - Info.plist
          - EDPFSKitExtension.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.edp.drive.fskit-poc.extension
        PRODUCT_NAME: EDPFSKitExtension
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Extension/Info.plist
        CODE_SIGN_ENTITLEMENTS: Extension/EDPFSKitExtension.entitlements
        SKIP_INSTALL: YES
        APPLICATION_EXTENSION_API_ONLY: YES
